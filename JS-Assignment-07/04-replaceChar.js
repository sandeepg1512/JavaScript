/* Input a String S with a word, and replace character “a” with “x” and
display the result -
Input: “apple” Output: “xpple”
*/

let str = "apple";

str = str.replace('a', 'x');
console.log(str);